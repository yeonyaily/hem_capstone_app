import 'package:flutter/material.dart';

class CheckupView extends StatelessWidget {
  const CheckupView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('CheckupView'),
    );
  }
}
