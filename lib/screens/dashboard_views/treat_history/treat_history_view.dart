import 'package:flutter/material.dart';

class TreatHistoryView extends StatelessWidget {
  const TreatHistoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('TreatHistoryView'),
    );
  }
}
